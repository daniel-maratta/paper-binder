# Task Log - 2026-W10

Append-only notes and discoveries for the week.

## Notes
- (empty)
- `2026-07-16`: Defer the unrelated tenant-host browser-form drift around the users-route `Temporary password` label into `T-0037` so it is handled with the broader known warnings/issues pass rather than reopening `T-0035`.
- `2026-07-16`: Treat any remaining build warnings, browser-suite warnings, and dependency or vulnerability advisories as `T-0037` close-out work unless a specific item is promoted earlier as its own blocking slice.
- `2026-07-17`: Reconciled the canonical taskboard to current repo truth: `T-0034`, `T-0035`, and `T-0036` are now recorded as complete, and `T-0037` is the remaining active `v1.1` close-out lane.
- `2026-07-17`: `T-0037` inventory pass results: `build.ps1 -Configuration Release` completed with `0` .NET warnings; `test.ps1 -Configuration Release -DockerIntegrationMode Require` passed all current frontend, unit, and integration coverage; `run-browser-e2e.ps1` passed both root-host and tenant-host suites without reproducing browser drift; `dotnet list PaperBinder.sln package --vulnerable --include-transitive` found no NuGet vulnerabilities; `npm audit` still reports `7` frontend advisories (`4` high, `1` moderate, `2` low) across `react-router-dom`/`react-router`, `vite`, `undici`, `postcss`, `esbuild`, and `@babel/core`, all with fixes available.
- `2026-07-18`: Added the `T-0037` controlled-copy strategy and inventory artifacts under `docs/95-delivery/`, landed the current copy pass across the public and authenticated frontend surfaces, and refreshed the affected component/E2E coverage to match the new `view as` and product-copy language.
- `2026-07-18`: Re-ran the canonical close-out validation on the current branch: `validate-checkpoint.ps1 -Configuration Release -DockerIntegrationMode Require` passed after disabling the local challenge bypass as required, and `run-browser-e2e.ps1` passed both root-host and tenant-host browser suites. The local `.env` default is now bypass-off, with `scripts/set-local-challenge-bypass.ps1` added as the repo-native opt-in toggle.
- `2026-07-16`: Pre-reconciliation documentation audit matrix for `T-0036`:

| Surface | Pre-reconciliation current-branch truth | `feature/public-site-copy-ux-refresh` truth | Keep / Change / Remove | Why | Reconciliation State |
| --- | --- | --- | --- | --- | --- |
| `.env.example` | Missing `PAPERBINDER_LEASE_RECENT_ACTIVITY_GRACE_SECONDS` even though runtime supports it with default `180` and docs mention it | Same omission | `Change` | Example config should expose the documented cleanup-retention setting. | `Addressed` |
| `docker-compose.e2e.yml` | Inherits `...CHALLENGE_LOCAL_BYPASS_ENABLED=false` defaults from base compose and uses the explicit E2E Turnstile script path | Older branch redundantly set both bypass flags to `"false"` | `Keep` | Current file matches `main`; explicit false flags would be redundant unless we later want extra defensive self-documentation. | `Addressed` |
| `docs/00-intent/demo-narrative.md` | Still drifted toward generic landing/login wording | Captured the product-first landing plus dedicated `/start-demo` entry flow | `Change` | The implemented public flow uses a landing page that leads into a distinct demo-entry route. | `Addressed` |
| `docs/00-intent/project-scope.md` | Still claimed a fixed post-expiry hard-delete target | Also needed a cleanup-retention truth pass | `Change` | Scope docs should describe immediate fail-closed behavior plus eventual cleanup with recent-activity deferral. | `Addressed` |
| `docs/00-intent/success-criteria.md` | Still claimed expired tenants are deleted within 5 minutes | Older branch did not reconcile this file either | `Change` | Success criteria must align with the shipped best-effort cleanup behavior rather than a fixed SLA promise. | `Addressed` |
| `docs/10-product/README.md` | Simplified lane guide hides the active `v1.1` presentation canon and historical/reference posture | Explicitly linked active presentation canon and marked historical V1 docs | `Change` | Product-lane navigation should point readers at the active canon and distinguish historical references. | `Addressed` |
| `docs/10-product/accessibility.md` | Simplified to generic `V1` baseline wording | Preserved the current baseline plus required post-upgrade audit posture and toast accessibility note | `Change` | The branch still needs the broader post-redesign accessibility pass and the docs should say so. | `Addressed` |
| `docs/10-product/component-specification-v1.md` | Simplified back toward generic V1 wording | Narrowed through the approved `v1.1` canon and captured toasts / lease-window semantics | `Change` | The current UI surface includes toast feedback and server-authoritative lease-window messaging. | `Addressed` |
| `docs/10-product/information-architecture.md` | Simplified back to `/`-owns-provisioning wording and understated document-detail behavior | Captured product-first landing plus `/start-demo` split and same-route users surface | `Change` | The current root-host route composition is the old-branch version, and document detail now defaults to a rendered preview with a source toggle. | `Addressed` |
| `docs/10-product/ux-notes.md` | Status regressed to plain `V1` | Explicitly marked as historical shipped-reference material | `Change` | This doc is not the active forward canon and should not read like it is. | `Addressed` |
| `docs/10-product/prd.md` | Mostly current, but provisioning wording became too loose (`landing page`) | More precise `Start Demo` framing | `Change` | The landing page leads in, but the live provision/login work happens on `/start-demo`. | `Addressed` |
| `docs/10-product/domain-nouns.md` | Lost binder-local document-title uniqueness, understated the document-detail presentation, and still implied a fixed cleanup target | Carried title uniqueness, but not the latest cleanup-retention nuance | `Change` | Runtime now enforces title uniqueness, defaults document detail to rendered markdown preview with source fallback, and uses eventual cleanup with recent-activity retention. | `Addressed` |
| `docs/10-product/user-stories.md` | Lease-cleanup slice still promised fixed 5-minute deletion | Older branch updated create-user credentials but not the lease-cleanup wording | `Change` | Acceptance criteria should describe fail-closed expiry plus prompt best-effort cleanup with recent-activity retention. | `Addressed` |
| `docs/20-architecture/frontend-app-route-map.md` | Document route still described source-only rendering | Older branch had the same stale route note | `Change` | The shipped document-detail route defaults to rendered markdown preview and allows switching to stored source. | `Addressed` |
| `docs/20-architecture/frontend-spa.md` | Simplified back to `/`-owns-provisioning wording and understated document detail | Captured product-first landing plus `/start-demo` split | `Change` | SPA host-context docs should match the live route registry and current document preview/source behavior. | `Addressed` |
| `docs/20-architecture/system-overview.md` | Still promised a fixed 5-minute hard-delete SLA | Older branch did not reconcile this file either | `Change` | Architecture overview should reflect immediate expiry denial and eventual worker purge with grace-based deferral. | `Addressed` |
| `docs/20-architecture/tenancy-model.md` | Still promised a fixed 5-minute hard-delete SLA | Older branch did not reconcile this file either | `Change` | Core tenancy model docs must align with current lease-cleanup behavior. | `Addressed` |
| `docs/20-architecture/tenancy-resolution.md` | Stale: still references `TENANT_HOST_UNAVAILABLE` and older flattening behavior | Also stale against current code | `Change` | Current middleware returns `TENANT_NOT_FOUND`, `TENANT_FORBIDDEN`, and `TENANT_EXPIRED` with `terminalTenantState` when applicable. | `Addressed` |
| `docs/20-architecture/demo-tenant-lease.md` | Generally current | Slightly richer note about `canExtend`-driven UI state | `Change` | Minor truth pass to keep browser-derivation rules explicit. | `Addressed` |
| `docs/20-architecture/worker-jobs.md` | Current selection/guard rules, but SLA wording still reads like a fixed 5-minute target | Similar | `Change` | Cleanup timing should remain eventual/prompt best-effort, not a hard threshold. | `Addressed` |
| `docs/30-security/secrets-and-config.md` | Dropped `PAPERBINDER_LEASE_RECENT_ACTIVITY_GRACE_SECONDS` from the canonical config surface | Explicitly listed and explained the key | `Change` | Security/config docs and `.env.example` should agree on the cleanup-retention setting. | `Addressed` |
| `docs/40-contracts/api-contract.md` | Current tenant-user create contract is correct, but lease endpoint failure semantics need to stay precise about `403` vs `404` vs `410` | Older branch had stale tenant-host flattening wording in other areas | `Change` | API docs must track the live middleware and route behavior exactly. | `Addressed` |
| `docs/70-operations/cleanup-jobs-runbook.md` | Already aligned with grace-window cleanup behavior and key naming | Equivalent | `Keep` | The runbook is already truthful and did not need further change in this pass. | `Addressed` |
| `docs/05-taskboard/tasks/T-0035-tenant-host-failure-externalization-and-trusted-expiry-recovery.md` | Read as if its transient flattening behavior were still the current repo contract | Same historical slice framing | `Change` | Taskboard history should note when a slice outcome was later superseded so it does not contradict current canonical docs. | `Addressed` |

## Decisions Captured Elsewhere
- Link to ADRs or canonical decisions as needed.
- `2026-07-24`: `T-0045`'s discovery phase is complete. Full findings persisted in
  `docs/50-engineering/t-0045-engineering-security-architecture-review.md`. Zero Critical/High
  findings; no cross-tenant data access, auth/authz bypass, CSRF gap, or exploitable XSS/injection
  found. Confirmed the `T-0044`-baseline `e2e/tenant-host.spec.ts` failure is a genuine stale-test
  defect (concrete fix recorded, not yet applied). Re-ran dependency scans: NuGet clean;
  `npm audit` still 7 advisories, but only `react-router-dom` is production-relevant (the rest are
  dev-tooling-only) and its fix is a major-version migration, not a patch. Confirmed the
  `TenantImpersonationBanner` component remains dead code (recommend removal). Confirmed the
  tenant-host users-route "Temporary password" browser-form-drift note from `2026-07-16` is already
  resolved by `T-0037`'s copy pass — no further action needed on that item. No code, test, or
  dependency changes were made; `T-0045` status moved to `active` (not `done`) pending a
  remediation pass and three flagged owner decisions (archive/unarchive UI, React Router migration
  deferral, `TenantImpersonationBanner` removal).
