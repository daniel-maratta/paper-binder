# Compatibility Shim

Agent plans were moved from `.agents/plans/` to `docs/55-execution/stages/`.

Vendor-agnostic agent workflows now live in:
- `docs/55-execution/workflows/`

This directory remains only as a path-compatibility marker.
